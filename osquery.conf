{
  "schedule": {
    "tls_proc": {
      "interval": 3,
      "query": "select pid, name, uid, resident_size from processes order by resident_size desc limit 3;"
    }
  },
  "node_invalid": false
}
